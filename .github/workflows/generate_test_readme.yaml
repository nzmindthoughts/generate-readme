name: Generate Test README file
on:
  push:
    paths:
      - 'config/account_info.test.json'
    branches:
      - test
permissions:
      id-token: write
      contents: write
      pull-requests: write
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
jobs:
  GenerateReadMe:
    runs-on: ubuntu-latest
    steps:
      - name: Generate README Update
        uses: ./.github/actions/generate-readme
        with:
          environment-name: 'test'
          target-branch: 'test'